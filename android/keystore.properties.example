# Copy to keystore.properties and set paths (do not commit keystore.properties).
storeFile=../New folder/fleet-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=fleet-release
keyPassword=YOUR_KEY_PASSWORD

# Ensure requirements.txt is always stored with LF (no CRLF) so pip on Linux works
requirements.txt text eol=lf
# Shell scripts: Linux/Render
scripts/render_migrate.sh text eol=lf
.flaskenv text eol=lf

# Sensitive credentials / temp folders
New folder/
*firebase-adminsdk*.json
google-services.json

# Local / env
.env
.env.local
*.db
venv/
.venv/
env/

# Render CLI binaries (large; use scripts/download_render_cli.ps1 after clone)
tools/render-cli/*.exe

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.eggs/
*.egg-info/
dist/
build/

# Uploads (optional: remove this line if you want to backup uploads)
uploads/

# Fleet Personal PC built assets (keep README; remove these lines if you commit full static export)
static/fleet_personal_pc/*
!static/fleet_personal_pc/README.txt

# Nested git backup after merging daedalOS (never commit)
daedalOS/.git_upstream_backup/

# Node
node_modules/

# Firebase (secret keys)
firebase-service-account.json

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Playwright browser binaries (downloaded on Render during build)
.playwright-browsers/
